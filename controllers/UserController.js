const postUser = async (request, response) => {
    try {
        const email = request.data.email;
        const password = request.data.email;
    } catch (error) {
        
        return error;
    }
}

